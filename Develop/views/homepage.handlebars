{{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a class="text-decoration-none post_title text" href="/post/{{post.id}}">{{post.title}}</a>
    </h2>
    <p class="date">Created on {{format_date post.date_created}}</p>
  </div>
</div>
{{/each}}